# 3D 전환 계획 — 저폴리 스타일라이즈드 (B안)

2D 프로토타입(`DEVELOPMENT_PLAN.md` Phase 0~6)이 동작하는 상태에서 출발한다.
이 문서는 **에셋 방향 B(저폴리 스타일라이즈드)** 로 3D 전환할 때의 순서와 완료 조건만 담는다.
설계 원칙은 §18을 그대로 따른다 — 특히 **로직에서 UnityEngine 분리**(§18-1)와
**손으로만 되는 배치 금지**(§18-9)는 3D에서도 깨지 않는다.

---

## 0. 전제

### 그대로 가는 것 (런타임 5,543줄 기준)

| 구분 | 규모 | 3D에서 |
|---|---|---|
| 순수 추리 로직 35파일 | 2,819줄 | **무변경** — 시간·스케줄·목격 판정·대화 조립·타임라인 복원·채점·추리 가능성 검증 |
| UI 8파일 | 1,229줄 | 거의 무변경 — uGUI 스크린 스페이스는 차원과 무관 |
| 오디오·데이터 로더 | 366줄 | 무변경 |
| EditMode 테스트 90개 | — | **계속 통과해야 한다.** 깨지면 로직에 Unity 의존이 새어 들어간 것이다 |

### 다시 쓰는 것

| 대상 | 규모 | 작업 |
|---|---|---|
| `SceneBuilder` | 471줄 | 3D 껍데기 생성기로 재작성 — 가장 큰 덩어리 |
| `NPCController` · `NpcDirector` | 247줄 | NavMesh 기반으로 |
| `ArtLibrary` · `ProceduralTextures` | 442줄 | 스프라이트 대체물 → 머티리얼. `ProceduralTextures`는 대부분 폐기 |
| `PlayerController` · `PlayerInteraction` · `InspectableObject` · `CameraFollow` | 251줄 | 2D 물리 → 3D 물리. 기계적 치환 |
| `RoomLabels` · `WorldLabel` | 84줄 | 월드 라벨 → 빌보드 이름표 |

### B안을 고른 대가 — 먼저 풀어야 할 문제

**이 게임의 핵심 루프는 여섯 방을 한 화면에 놓고 보는 것이다.** T키 타임라인 관찰 모드는
7개 시각에 걸쳐 모든 방의 모든 인물을 동시에 보여 주고, 플레이어는 거기서 증언과 목격의 모순을 찾는다.

B안은 벽을 사람 키(약 3 m)로 세우므로 **방끼리 서로 가린다.** 이건 취향 문제가 아니라 기능 문제다.
Phase 3D-2에서 반드시 해결하고 넘어간다. 해결 못 하면 B안을 포기하고 A안(허리 높이 컷어웨이)으로 내린다.

---

## 1. 에셋 목록 (B안)

### 라이선스 기준

지금 저장소에 든 에셋은 **전부 CC0/퍼블릭 도메인**이다(`CREDITS.md`). 3D에서도 이 기준을 유지한다.
CC BY는 게임 안 결과 화면에도 표기해야 하므로 되도록 피한다.

| 분류 | 필요량 | CC0 후보 |
|---|---|---|
| 건축 모듈 | 20~25 | [Kenney](https://kenney.nl/assets) (CC0), [Quaternius](https://quaternius.com/) (CC0) |
| 가구·소품 | 50~70 | Quaternius Interior, KayKit (팩별 확인) |
| 인물 | 5 + 플레이어 | Quaternius Universal Animation Library (CC0, 리그 공유) |
| 애니메이션 | 대기·걷기·조사 | 위 라이브러리 또는 Mixamo(무료, 라이선스 페이지 확인) |
| 단서 히어로 프롭 | 7 | 직접 제작 권장 — 가까이서 조사하므로 세트 드레싱보다 디테일이 필요 |

**건축 모듈 최소 구성**: 벽(1 m·2 m·4 m), 코너, 문틀, 문짝, 바닥 타일, 걸레받이, 창, 벽 장식 2~3종.
`rooms.json`의 벽 두께 0.4 m와 문 폭 2.0 m에 맞춰 고르거나 스케일을 맞춘다.

**방별 소품 예산** (여섯 방이 서로 다르게 읽히는 데 필요한 최소):

| 방 | 소품 |
|---|---|
| 로비 | 접수대, 코트걸이, 괘종시계, 전화대, 거울, 소파 |
| 식당 | 긴 식탁, 의자 8, 사이드보드, 샹들리에, 식기, 촛대 |
| 창고 | 선반, 상자, 통, 자루, 공구, 단지 |
| 복도 | 러너 카펫, 벽등, 액자, 협탁 |
| 서재 | 책상, 의자, 책장, 책, 지구본, 벽난로, 안락의자, 램프 |
| 손님 방 | 침대, 협탁, 옷장, 화장대, 여행 가방 |

---

## 2. Phase별 계획

### Phase 3D-0 — 렌더 파이프라인 결정

저폴리 평면 셰이딩과 색 보정을 쓰려면 URP가 유리하다. 다만 파이프라인을 바꾸면 **셰이더 집합이 통째로 달라진다.**

* URP 패키지 도입, 파이프라인 에셋 생성(에디터 스크립트로 — 손으로 만들지 않는다)
* `RuntimeShaderSetup.RequiredShaders`를 URP 셰이더로 갱신
* uGUI는 URP에서 그대로 동작한다. `UIFactory`는 손대지 않는다

> **주의.** 2D 빌드에서 `UI/Default`가 빠져 화면이 통째로 마젠타로 나온 적이 있다.
> 에디터 Play로는 절대 안 잡히고 **빌드해서 실행해야만** 드러난다.
> 파이프라인을 바꾸면 같은 일이 다시 일어난다고 보고, 이 Phase의 완료 조건에 빌드 실행을 넣는다.

**완료 조건**
- `error CS` 0건
- EditMode 90개 통과
- 기존 2D 씬을 URP로 열어 빌드 → 실행 → 마젠타 없음

---

### Phase 3D-1 — 껍데기 생성기 (`SceneBuilder3D`)

`rooms.json`에서 3D 벽·바닥·문틀을 세운다. **손 배치 없음**(§18-9).

* `RoomLayout.BuildAllWallSegments()`는 순수 C#이라 **그대로 재사용한다** — 방 테두리에서 문을 빼고
  같은 직선 위 조각을 병합하는 로직이 이미 있다. 2D에서 쓰던 벽 조각 목록이 3D 벽 배치 목록이 된다
* 벽 조각 길이에 맞춰 모듈 프리팹을 반복 배치. 문 자리에는 문틀 + 문짝
* 바닥은 방마다 타일 반복, 천장은 없음(위에서 내려다보므로)
* 기존 `SceneBuilder`는 지우지 않고 남겨 둔다 — 2D로 되돌릴 수 있어야 한다

**완료 조건**
- batchmode `-executeMethod`로 씬 생성 성공
- 방 6개·문 5개가 `rooms.json` 좌표와 일치
- `rooms.json`의 방 크기를 바꾸고 다시 돌리면 3D 껍데기가 따라 바뀐다

---

### Phase 3D-2 — 카메라와 가림 (B안의 핵심 위험)

**여기서 막히면 B안을 포기한다.** 먼저 한다.

* **디오라마 카메라**: 높은 각도(약 45~55°)에서 내려다보고, 90° 단위로 회전. 1인칭·어깨 너머 금지
* **벽 페이드**: 카메라와 플레이어 사이를 가리는 벽을 투명하게. 디더 페이드 셰이더 또는 방 단위 알파
* **타임라인 모드**: T키를 누르면 카메라가 더 올라가고 **모든 벽이 낮은 알파로** 내려가 여섯 방이 동시에 보인다.
  사실상 A안(디오라마)으로 전환되는 셈이다 — B안은 탐색 중에만 벽을 세운다
* 인물 식별색을 머리 위 빌보드 또는 발밑 링으로 유지. 2D에서 색으로 구분되던 가독성을 잃지 않는다

**완료 조건**
- 여섯 방 모두에서 플레이어가 벽에 가려지지 않는다
- 타임라인 모드에서 인물 5명이 어느 방에 있는지 한 화면에서 판별된다
- 사람이 직접 확인 — 이 Phase는 자동 검증이 불가능하다

---

### Phase 3D-3 — 이동 · 상호작용

* `Rigidbody2D` → `CharacterController`, `Collider2D` → `Collider`,
  `Physics2D.OverlapCircleAll` → `Physics.OverlapSphere`
* NavMesh: `com.unity.ai.navigation` 2.0.13이 **이미 `manifest.json`에 있다**.
  `NavMeshSurface`를 생성 씬에 붙이고 baked를 스크립트로 굽는다(손으로 굽지 않는다)
* `NPCController`의 `Vector3.MoveTowards` 경로 추종을 `NavMeshAgent`로 교체.
  틱이 바뀌면 목적지 방으로 경로 요청, 틱 안에 도착 못 하면 순간이동으로 보정(지금 `TeleportTo`가 하는 역할)

**완료 조건**
- EditMode 90개 계속 통과
- 플레이어가 복도를 통해 여섯 방을 모두 오갈 수 있다
- 틱 0~6을 돌렸을 때 인물 5명이 스케줄과 같은 방에 있다 (F9 실제 스케줄 보기로 대조)

---

### Phase 3D-4 — 인물

* 인물 5 + 플레이어. 리그 공유, 애니메이션은 대기·걷기·조사 3종이면 충분
* `npcs/*.json`의 `color`를 의상 악센트 또는 발밑 링에 그대로 사용
* `WorldLabel`(TextMesh) → 빌보드 이름표. 카메라를 향하도록

**완료 조건**
- 각 틱에서 올바른 인물이 올바른 방에, 올바른 색으로
- 대화(E)가 2D와 동일하게 동작 — `DialogueUI`는 건드리지 않았으므로 그대로여야 한다

---

### Phase 3D-5 — 소품 · 단서

* **`props.json` 신설** — 방 id, 좌표, 회전, 프리팹 id. 가구도 데이터로 둬야 §18-9가 유지되고
  에이전트가 GUI 없이 배치를 고칠 수 있다
* 단서 7개는 각각 구분되는 메시. 접근하면 강조
* `GameDataValidator`에 프롭 참조 검사 추가 — 없는 방·없는 프리팹을 가리키면 씬 생성이 실패하도록

**완료 조건**
- 단서 7개 전부 조사 가능, 수사 노트에 기록됨
- `props.json`의 좌표를 바꾸고 씬을 다시 만들면 따라 움직인다
- 데이터 검증이 잘못된 참조를 잡아낸다

---

### Phase 3D-6 — 조명 · 분위기

* 라이트맵 베이크 + 방별 램프 포인트 라이트. 베이크는 스크립트로 굽는다
* 포스트 프로세싱: 비네팅, 색 보정으로 "어두운 저택" 톤 유지
* 지금 `art.json`의 방별 `tint`는 3D에서 **조명 색**으로 옮긴다 — 바닥 색을 곱하던 역할을 조명이 대신한다

**완료 조건**
- 빌드 실행에서 목표 프레임레이트 유지
- 여섯 방이 서로 다른 분위기로 읽힌다

---

### Phase 3D-7 — UI 재연결 · 빌드 검증

* UI는 대부분 그대로다. `ArtLibrary`의 초상화·단서 그림 로딩 경로는 유지
* `RuntimeShaderSetup`을 URP 셰이더 목록으로 최종 확인
* 시작 → 조사 → 대화 → 타임라인 → 고발 → 결과 전체 흐름 플레이

**완료 조건**
- **standalone 빌드를 실행해** 처음부터 끝까지 플레이 가능
- 마젠타 없음, 런타임 예외 0건 (`Player.log` 확인)

---

## 3. 위험 요소

| 위험 | 징후 | 대비 |
|---|---|---|
| **B안의 가림 문제** | 타임라인에서 방이 서로 가려짐 | Phase 3D-2를 먼저. 실패하면 A안으로 내린다 |
| **셰이더 스트리핑** | 빌드 실행 시 마젠타 | 파이프라인 변경 때마다 **빌드해서 실행**. 에디터 Play로는 안 잡힌다 |
| 라이트맵 베이크 시간 | batchmode가 오래 걸림 | 개발 중에는 실시간 조명, 마무리에만 베이크 |
| NavMesh 수동 베이크 | 씬을 다시 만들면 NavMesh가 사라짐 | 생성 스크립트 안에서 굽는다 |
| 로직 오염 | EditMode 테스트가 깨짐 | 매 Phase 완료 조건에 테스트 통과를 넣었다. 깨지면 Unity 의존이 새어 든 것 |
| 에셋 라이선스 혼입 | CC BY 에셋이 섞임 | 받을 때마다 `CREDITS.md`에 한 줄. CC0 아닌 건 결과 화면 표기 필요 |

---

## 4. 일정 감각

1인 기준 **4~7주**. 순서를 바꾸지 않는 것이 중요하다 — Phase 3D-2가 B안의 생사를 가르므로
소품과 조명에 시간을 쓰기 전에 끝내야 한다.

| Phase | 분량 |
|---|---|
| 3D-0 파이프라인 | 2~3일 |
| 3D-1 껍데기 생성기 | 4~6일 |
| **3D-2 카메라·가림** | **4~7일 (최대 위험)** |
| 3D-3 이동·NavMesh | 3~4일 |
| 3D-4 인물 | 4~6일 |
| 3D-5 소품·단서 | 5~8일 |
| 3D-6 조명 | 3~5일 |
| 3D-7 UI·빌드 | 2~3일 |

---

## 5. 먼저 할 것 — 수직 슬라이스

전체 Phase에 들어가기 전에 **서재 한 방만** 3D로 만들어 본다.
방 1개 + 인물 1명 + 단서 1개 + 디오라마 카메라 + 벽 페이드.

이것만으로 B안의 가장 큰 위험(가림)과 가장 큰 비용(소품 밀도)이 동시에 드러난다.
여기서 나온 실측치로 위 일정을 다시 잡는다. **1주 이내**로 끝내고, 아니면 A안을 재검토한다.
