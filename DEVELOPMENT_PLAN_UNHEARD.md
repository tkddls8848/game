# 엿듣기 추리로 전환 — 개발 계획

플레이어가 조종하는 것이 **인물이 아니라 청취점**이 된다.
이미 벌어진 한 시간의 기록 위에서 평면도를 돌아다니며, 가까운 곳의 대화만 듣고,
몇 번이고 되돌려 재생해 사건을 재구성한다. 인물은 익명이고 **누가 누구인지 알아내는 것 자체가 퍼즐**이다.

`DEVELOPMENT_PLAN.md`(2D 인터뷰 프로토타입)의 §18 설계 원칙은 그대로 간다.
**단, 원칙 5번(정수 틱 0~6)은 이 전환에서 바뀐다** — Phase U-0 참고.

> **`DEVELOPMENT_PLAN_3D.md`는 보류한다.** 엿듣기 추리는 평면도 뷰가 곧 게임 화면이고,
> 지금 2D 프로토타입이 이미 그 카메라다. 3D 전환(4~7주)은 이 방향에서 필요 없다.

---

## 0. 결정된 것

| | |
|---|---|
| 발화 전달 | **대사 버블**. 소리 없이, 청취점 근처의 발화만 화면에 뜬다 |
| 사건 | **새로 쓴다**. 엿듣기를 전제로 처음부터 설계 |

대사 버블로 먼저 가는 이유는 **퍼즐 구조를 가장 싸게 검증하기 위해서**다.
대본을 고치는 비용이 0이라 반복이 빠르고, 헤드리스로 기계 검증도 된다.
소리는 대본이 확정된 뒤에 얹을 수 있게 데이터 형식에 자리만 비워 둔다(`clip` 필드).

---

## 1. 지금 코드에서 살아남는 것

| 모듈 | 규모 | |
|---|---|---|
| `RoomLayout` | 295줄 | **그대로.** 평면도가 곧 게임 화면이다 |
| `AccusationForm` · `CaseGrader` | 154줄 | **그대로.** 마지막 질문 단계가 정확히 이 구조다 |
| `NpcRoster` · `GameDataValidator` | 354줄 | 거의 그대로 |
| `KoreanText` · `ModalState` · `GameEvents` · `Interval` · `Point2` · `WallSegment` | — | 그대로 |
| UI 8파일 | 1,229줄 | 껍데기 재사용. 수사 노트가 **인물 식별 보드**가 된다 |
| `SceneBuilder` · 연출 에셋 | — | 그대로. 바닥·벽·초상화·폰트 전부 쓴다 |

## 2. 버리거나 다시 쓰는 것

| 모듈 | 규모 | |
|---|---|---|
| `ConversationBuilder` | 114줄 | **폐기.** 말을 거는 인터뷰가 없다 |
| `SightingGenerator` | 71줄 | **폐기.** 목격이 아니라 엿듣기다 |
| `DialogueCatalog` | 34줄 | 시간표 대본 로더로 교체 |
| `NpcSchedule` | 47줄 | 틱당 방 1개 → 연속 이동 트랙 |
| `CaseSolvabilityChecker` | 165줄 | 판정 기준 자체가 달라진다(§5) |
| `TimelineBoard` | 169줄 | 재구성 보드로 용도 변경 |
| `NotebookPresenter` | 203줄 | 식별 보드로 개조 |

---

## 3. 데이터 형식

### 대본 (`cases/case_02/script.json`)

```json
{
  "caseId": "case_02",
  "durationMs": 600000,
  "speakers": [
    { "voiceId": "v1", "npcId": "npc_a" },
    { "voiceId": "v2", "npcId": "npc_b" }
  ],
  "utterances": [
    { "id": "u001", "startMs": 12400, "durationMs": 3200,
      "voiceId": "v1", "room": "room_dining", "clip": "",
      "text": "그 이야기는 여기서 하지 말아요." }
  ]
}
```

* `speakers`가 **목소리 → 실제 인물** 대응표다. 플레이어에게는 숨기고 채점에만 쓴다
* 화면에는 `voiceId`만 나온다. 플레이어가 이름을 배정하면 그 배정이 채점 대상이 된다
* `clip`은 비워 둔다. 나중에 소리를 얹을 때 쓸 자리다
* `startMs`·`durationMs`는 **정수 밀리초**. 부동소수 비교 금지(원칙 5번의 정신은 유지)

### 이동 트랙 (`npcs/*.json`에 추가)

```json
"track": [
  { "atMs": 0,      "room": "room_dining" },
  { "atMs": 95000,  "room": "room_hall" },
  { "atMs": 131000, "room": "room_victim" }
]
```

틱당 방 1개였던 `schedule[7]`을 대체한다. 두 구간 사이는 보간해 토큰을 움직인다.

---

## 4. 가청 판정

복잡한 음향 모델은 만들지 않는다. 방 단위로 끊는다.

| 청취점 위치 | 결과 |
|---|---|
| 발화자와 **같은 방** | 대사 전문이 보인다 |
| **문으로 이어진 옆 방** | 웅얼거림 — 누군가 말하고 있다는 표시만, 내용은 안 보인다 |
| 그 밖 | 아무것도 없다 |

웅얼거림이 중요하다. "저기서 무슨 말이 오가는데 여기선 안 들린다"가 다시 들으러 가게 만드는 동력이다.
방 인접 여부는 `rooms.json`의 `doors`에서 이미 나온다 — 새 데이터가 필요 없다.

---

## 5. 새 추리 가능성 검사

지금 `CaseSolvabilityChecker`는 "범행 시각에 범인만 알리바이가 없고 모순이 2건 이상"을 본다.
엿듣기에서는 기준이 달라진다. 셋을 기계로 검사한다.

1. **풀 수 있는가** — 정답 6항목이 각각 어딘가에서 들리는 발화로부터 도출되는가
2. **한자리에서는 못 푸는가** — 어떤 고정 위치에 1회차 내내 서 있어도 전부 들을 수 없는가.
   이게 깨지면 되돌려 들을 이유가 사라진다
3. **익명이 풀리는가** — 목소리 5개가 각각 최소 1번은 이름이 불리거나 정체를 특정할 발화를 갖는가

순수 C#으로 쓴다. 대본을 고칠 때마다 EditMode 테스트가 이걸 돌린다.

---

## 6. Phase

### Phase U-0 — 시간 축 교체

`GameTime`을 정수 밀리초로 바꾼다. 가장 깊고 가장 먼저다.

* 틱은 표시용 파생값으로 남긴다(노트·타임라인 눈금)
* 기존 EditMode 테스트 중 틱을 전제한 것들을 함께 고친다
* **CLAUDE.md 설계 원칙 5번을 고쳐 쓴다** — "정수 틱 0~6" → "정수 밀리초, 부동소수 시각 비교 금지"

**완료 조건**: 컴파일 `error CS` 0건, EditMode 전부 통과

### Phase U-1 — 대본과 재생

* `script.json` 스키마·로더·참조 검증
* 연속 재생 장치 — 재생·일시정지·탐색·재시작·배속

**완료 조건**: 헤드리스로 "시각 T에 재생 중인 발화 목록"을 뽑아낼 수 있다. 테스트로 검증

### Phase U-2 — 청취점과 가청 판정

* 플레이어가 귀를 옮긴다. 같은 방·옆 방·그 밖 세 단계
* 발화 버블 UI, 웅얼거림 표시

**완료 조건**: 같은 시각에 위치만 바꾸면 보이는 발화가 달라진다

### Phase U-3 — 인물 이동과 익명화

* 연속 이동 트랙으로 토큰이 움직인다
* 화면에는 익명 토큰. 수사 노트를 **목소리 → 이름 배정 보드**로 개조

**완료 조건**: 배정한 이름이 기록되고 채점에 반영된다

### Phase U-4 — 새 사건 대본

* 150~300 발화. 겹침과 가림이 설계된 상태
* §5의 검사 셋을 통과

**완료 조건**: 검사기가 "풀 수 있고, 한자리에선 못 푼다"를 기계 판정

### Phase U-5 — 질문 단계와 결과

* 기존 `AccusationForm`·`CaseGrader` 연결. 목소리 배정 정답 여부를 채점에 추가

**완료 조건**: standalone 빌드로 처음부터 끝까지 플레이

### Phase U-6 — 다듬기

배속, 놓친 발화 표시, 이미 들은 발화 표시, 재청취 편의.

---

## 7. 위험 요소

| 위험 | 대비 |
|---|---|
| **대본이 진짜 비용이다** | 지금 대사는 4명 23줄이다. 10배 이상이 필요하다. Phase U-4에 가장 긴 시간을 잡는다 |
| 시간 축 교체가 전방위로 번진다 | Phase U-0을 먼저 끝내고 테스트로 고정한다 |
| 한자리에서 다 들려 버린다 | §5의 2번 검사가 기계로 잡는다. 사람 판단에 맡기지 않는다 |
| 놓친 발화를 영영 모른다 | Phase U-6에서 "이 방에서 못 들은 발화 있음" 표시. 없으면 답답함이 재미가 아니라 벽이 된다 |
| 버블을 읽는 것과 듣는 것은 다르다 | 겹치는 발화는 읽으면 동시에 처리된다. 난이도가 낮아진다는 뜻이니, 소리를 얹을 때 난이도를 다시 잡는다 |

---

## 8. 먼저 할 것 — 수직 슬라이스

전체 Phase 전에 **방 2개, 목소리 2개, 90초**짜리 최소 장면을 만든다.
같은 90초를 두 위치에서 들으면 서로 다른 것이 들리고, 둘을 합쳐야 한 가지 사실이 나오는 구조.

이것만으로 이 게임이 성립하는지가 드러난다. **3일 이내**로 끝낸다.
