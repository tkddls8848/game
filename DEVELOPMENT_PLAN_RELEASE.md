# 출시 판정 기준 — 무엇이 끝나야 끝난 것인가

`DEVELOPMENT_PLAN_UNHEARD.md`가 *무엇을 만들지*를 정한다면, 이 문서는 *언제 멈출지*를 정한다.
"완성도를 높인다"는 목표는 종료 조건이 없다. 아래 항목이 전부 체크되면 끝난 것으로 본다.

기준은 **1인 개발 인디 게임의 유료 출시**(Steam 등)다. AAA 기준이 아니다.

---

## 0. 지금 위치 (2026-09-25)

| 항목 | 값 |
|---|---|
| 런타임 코드 | 8,025줄 / 70파일 |
| EditMode 테스트 | **157/157 통과** (Unity 6000.0.81f1 batchmode) |
| 컴파일 | `error CS` 0 · `warning CS` 0 |
| 사건 대본 | 90초 수직 슬라이스 1개 (방 2 · 목소리 2 · 발화 13) |
| 완료 Phase | U-0 시간축 · U-1 재생 장치 · U-2 청취점/소나 |

---

## 1. 내가 끝낼 수 없는 것 — 먼저 밝힌다

아래는 작업량 문제가 아니라 **외부 의존**이다. 코드를 아무리 써도 해결되지 않는다.

### 1.1 한국어 음성 (가장 큰 구멍)

이 게임은 "음성 기반 추리"인데 지금 발화는 전부 텍스트 버블이고 `Utterance.clip` 자리는 비어 있다.
`AudibilityModel`이 이미 같은 방/옆 방을 가르고 `SonarText.Garble`이 웅얼거림을 만들지만,
**들리는 것이 없으면 이 게임의 전제가 성립하지 않는다.**

- CC0/퍼블릭 도메인으로 한국어 연기 음성을 구할 방법은 없다
- 합성음(TTS)은 라이선스가 상업 이용을 허용하는 것을 **구매**해야 하고, 그마저 연기 톤이 필요한
  추리 대사에는 부족할 수 있다
- 성우 섭외 시 목소리 5~8개 × 발화 150~300개. 이건 사람이 결정하고 돈을 쓰는 일이다

**결정이 필요하다**: 성우 녹음 / 상업 TTS 구매 / 텍스트 기반으로 장르를 재정의.
셋 중 무엇이든 정해지기 전까지 이 항목은 열려 있다.

### 1.2 그 밖

- 스토어 페이지, 스크린샷, 트레일러, 연령 등급, 결제·배포 설정
- 실제 플레이어 대상 QA (난이도가 맞는지는 남이 풀어 봐야 안다)
- 상표·상호 확인

---

## 2. 기계로 판정되는 것 — 여기는 끝까지 간다

각 항목은 `error CS` 0 · `warning CS` 0 · EditMode 전부 통과 상태를 유지해야 한다.

### 2.1 남은 Phase (DEVELOPMENT_PLAN_UNHEARD.md)

- [ ] **U-3 인물 이동과 익명화** — 연속 이동 트랙, 익명 토큰, 목소리→이름 배정 보드
- [ ] **U-4 사건 대본** — 150~300 발화. §5 검사 셋(풀 수 있다 / 한자리에선 못 푼다 / 익명이 풀린다) 통과
- [ ] **U-5 질문 단계와 결과** — `AccusationForm`·`CaseGrader` 연결, 목소리 배정 채점
- [ ] **U-6 다듬기** — 놓친 발화 표시, 이미 들은 발화 표시, 재청취 편의

### 2.2 출시에 필요한, 계획에 없던 것

- [ ] **저장·불러오기** — 회차 중간 저장, 들은 기록 보존
- [ ] **설정 화면** — 음량(BGM·환경음·효과음 개별), 창/전체화면, 해상도, 조작 안내
- [ ] **일시정지** — 지금 `Esc`는 창 닫기에만 쓰인다
- [ ] **첫 실행 안내** — 엿듣기는 익숙한 장르가 아니다. 조작과 목적을 처음에 알려야 한다
- [ ] **결과 후 흐름** — 재도전, 다른 사건 선택, 종료
- [ ] **빌드 위생** — 제품명·아이콘·버전, 로그 스팸 제거, 릴리스 빌드 실행 시 예외 0건

### 2.3 품질 게이트 (매 Phase 종료 시 전부 통과해야 한다)

- [ ] batchmode 컴파일 `error CS` 0 · `warning CS` 0
- [ ] EditMode 테스트 전부 통과 (회귀 0)
- [ ] `RebuildMainScene` 성공, 예외 0건
- [ ] standalone 빌드 성공 + 실행 시 `Player.log`에 예외 0건
- [ ] 새 데이터는 검증기가 받아들이고, 잘못된 데이터는 거부한다

---

## 3. 사람이 봐야 하는 것 — 기계로 대신할 수 없다

에이전트는 화면을 보지 못하고 소리를 듣지 못한다. 아래는 확인을 요청하고 기다린다.

- [ ] 웅얼거림이 *궁금하게* 만드는가 — 옆방으로 가 보고 싶어지는가
- [ ] 한 회차(수 분)를 되돌려 듣는 것이 지루하지 않은가
- [ ] 사건이 **풀리는데 너무 쉽지 않은가** — 검사기는 "풀 수 있다"만 보증하고 재미는 보증하지 않는다
- [ ] 소나 화면의 가독성 — 파문과 글자가 겹쳐 읽기 힘들지 않은가
- [ ] 음량 균형, 환경음 전환이 거슬리지 않는가

---

## 4. 작업 원칙 (병렬 작업 시)

**Unity batchmode는 프로젝트 락을 잡는다.** 동시에 두 개를 돌리면 실패한다.
따라서 작업을 이렇게 가른다.

| 종류 | 병렬 | 검증 방법 |
|---|---|---|
| 순수 C# 로직 (`using UnityEngine` 없음) | 가능 | mcs + NUnit 헤드리스 (§3.2-d) |
| 데이터·대본 JSON | 가능 | 순수 C# 검사기 |
| 문서 | 가능 | — |
| 씬·UI·빌드 | **불가** | Unity batchmode, 한 번에 하나 |

§18-1(로직에서 UnityEngine 분리)이 여기서 값을 한다. 이 원칙을 지킨 덕에 대부분의 작업이 병렬화된다.
깨뜨리면 병렬 작업이 직렬로 무너진다.
